public class Main {

	public static void main(String[] args) {
		
		Student st=new Student();
		Scanner sc=new Scanner(Sytem.in);
		st.getStudents();
		St.setStudents(sc.nextLine());
		st.getStudents(sc.nextInt());
		St.setStudent(sc.nextLine());
		st.getStudent(sc.nextInt());
		st.addFirst(sc.nextLine());
		st.addLast(sc.nextLine());
		st.add(sc.nextLine(),sc.nextInt());
		st.remove(sc.nextLine());
	
		
	}

}
